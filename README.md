backend-DEC
Backend for the app DEC (under Construction)

Project DEC

Technologies: 
Node.js 
Express 
Cors 
nodemon 

Base de datos:
MongoDB (usuarios, historial)
cloudinary (imágenes de detecciones)


Install dependencies: Run only what is inside the quotation marks (" ") in the terminal.

npm install <-//Dependencies

1- "npm install express" --EXPRESS 

2- "npm install cors"   --CORS 

3- "npm install -g nodemon"  --NODEMON 

4- "npm install mongoose"   --MONGOOSE

5- npm install multer   -- multipart form/data

6- npm install cloudinary   -- cloudinary package